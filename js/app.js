function sum(a, b) {
        
    let  Array = [];
    let  sumout = a +b;
    Array[0] = sumout
    let result = `The sum of ${a} and ${b} is ${sumout}.`
    Array[1] = result;
    return Array;
    }
    
    testSum(4, 7);


    